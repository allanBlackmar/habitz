import { View, Text } from 'react-native';
import styles from '../style/styles';

const DailyTaskLogScreen = () => {
    return (
        <View style={styles.titleContainer}>
            <Text style={styles.title}>Daily Task Log</Text>
        </View>
    );
};

export default DailyTaskLogScreen;
